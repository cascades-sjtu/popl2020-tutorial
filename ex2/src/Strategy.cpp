#include "Strategy.h"

/*
 * Implement your search strategy.
 */
void searchStrategy(z3::expr_vector &OldVec) {
  // give equal to the highest priority
//   for (int i = 0; i < OldVec.size(); i++) {
//     if (OldVec[i].is_eq()) {
//       z3::expr tmp = OldVec[0];
//       OldVec[0] = OldVec[i];
//       OldVec[i] = tmp;
//     }
//   }
}
